# OpenAI (ChatGPT mini)
OPENAI_API_KEY=your_openai_key
# Optional ein anderes Modell, Standard ist gpt-4.1-mini
OPENAI_MODEL=gpt-4.1-mini

# E-Mail (SMTP)
EMAIL_FROM="NewsAgent <newsagent@example.com>"
EMAIL_TO="you@example.com,you2@example.com"
# Optional JSON-based recipient configuration (preferred):
# Per-report/cadence secrets (lists or nested dicts):
#   RECIPIENTS_JSON_CYBERMED_DAILY, RECIPIENTS_JSON_CYBERMED_WEEKLY, RECIPIENTS_JSON_CYBERMED_MONTHLY
#   RECIPIENTS_JSON_CYBERLURCH_DAILY, RECIPIENTS_JSON_CYBERLURCH_WEEKLY, RECIPIENTS_JSON_CYBERLURCH_MONTHLY
# Per-report legacy:
#   RECIPIENTS_JSON_CYBERMED, RECIPIENTS_JSON_CYBERLURCH
# Combined config (nested) for both reports:
#   RECIPIENTS_CONFIG_JSON='{"cybermed":{"daily":["a"],"weekly":["b"],"monthly":["c"]},"cyberlurch":{"daily":["x"],"weekly":["y"],"monthly":["z"]}}'
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=newsagent@example.com
SMTP_PASS=replace_me

# Steuerung
SEND_EMAIL=1
MAX_ITEMS_PER_CHANNEL=5
DETAIL_ITEMS_PER_DAY=8
DETAIL_ITEMS_PER_CHANNEL_MAX=3

# FOAMed collection toggles
# Enable an additional lightweight HTML sampling pass (kept out of the email body; stats only)
FOAMED_AUDIT=0
# Force HTML fallback for specific FOAMed sources (comma-separated names)
FOAMED_FORCE_FALLBACK_SOURCES=""
